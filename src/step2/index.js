import { Root } from 'src/step2/Root';
import React from 'react';
import ReactDOM from 'react-dom'

const rootContainer = document.getElementById('react-root');

ReactDOM.render(<Root />, rootContainer);